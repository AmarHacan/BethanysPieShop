@model PieListViewModel;

<h1 class="my-9">@Model.CurrentCategory</h1>

<div class="row row-cols-1 row-cols-md-3 g-4">

    @foreach (var pie in Model.Pies)
    {
        <partial name="_pieCard" model="@pie" />
    }

</div>

